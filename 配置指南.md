# 学术 PKM 工作流配置指南

> 本指南覆盖你需要手动完成的全部配置。TRAE 端的 8 个 Skill 和 Obsidian vault 骨架已由 TRAE 自动创建完毕，你只需按以下步骤安装软件、配置插件、设置环境变量即可。

---

## 目录

1. [总览：已完成的 vs 需你操作的](#1-总览)
2. [Zotero 端配置（约 10 分钟）](#2-zotero-端配置)
3. [Obsidian 端配置（约 20 分钟）](#3-obsidian-端配置)
4. [TRAE 端配置（约 5 分钟）](#4-trae-端配置)
5. [验证：确认一切就绪](#5-验证)
6. [端到端使用示例](#6-端到端使用示例)
7. [常见问题](#7-常见问题)

---

## 1. 总览

### 已由 TRAE 自动创建（你无需操作）

| 内容 | 路径 |
|------|------|
| 8 个 TRAE Skill | `d:\Desktop\论文\.trae\skills\{skill-name}\SKILL.md` |
| 5 个 Python 脚本 | 各 Skill 的 `resources\` 目录下 |
| 4 个 resources 文档 | 各 Skill 的 `resources\` 目录下 |
| Obsidian vault 骨架 | `d:\Desktop\论文\obsidian-vault\` |
| 4 个 Templater 模板 | `obsidian-vault\Templates\` |
| 4 个 MOC Dataview 文件 | `obsidian-vault\MOC\` |
| 7 个 vault 目录 | `Literature\` `Concepts\` `MOC\` `Reviews\` `Templates\` `Scripts\` `Attachments\` |

### 需你操作

| 步骤 | 预计时间 | 章节 |
|------|---------|------|
| Zotero：装 Better BibTeX + ZotLit | 10 分钟 | [§2](#2-zotero-端配置) |
| Obsidian：装软件 + 4 个插件 + 配置 | 20 分钟 | [§3](#3-obsidian-端配置) |
| TRAE：申请 S2 API key + 设环境变量 | 5 分钟 | [§4](#4-trae-端配置) |
| 验证：跑通端到端流程 | 15 分钟 | [§5](#5-验证) |

---

## 2. Zotero 端配置

### 2.1 确认 Zotero 版本

1. 打开 Zotero → 菜单栏 `帮助` → `关于 Zotero`
2. 确认版本号 ≥ **7.0.15**（ZotLit 最新版要求）
3. 如版本过低：`帮助` → `检查更新`

### 2.2 安装 Better BibTeX 插件

**作用**：为每篇文献生成稳定的 citekey（如 `vaswani2017attention`），这是 ZotLit 和所有 Skill 识别论文的共同主键。

1. 访问 https://retorque.re/zotero-better-bibtex/installation/
2. 下载最新版 `.xpi` 文件
3. Zotero → `工具` → `插件` → 右上角齿轮 ⚙️ → `Install Add-on From File...`
4. 选择刚下载的 `.xpi` 文件 → 重启 Zotero
5. **刷新所有 citekey**：
   - 菜单栏 `编辑` → `首选项` → `Better BibTeX`
   - 确认 Citation Key 格式为 `auth.lower + shorttitle(3,3) + year`（推荐）
   - 点击 `Export` → 勾选 `Keep updated`
   - 菜单栏 `工具` → `Better BibTeX` → `Refresh BibTeX keys`（处理全部 1.4 万篇，可能需几分钟）

### 2.3 安装 ZotLit Zotero 端插件

**作用**：让 Obsidian 的 ZotLit 插件能通过本地 API 与 Zotero 通信，自动同步文献笔记。

1. 访问 https://github.com/PKM-er/obsidian-zotlit/releases
2. 下载最新版 Zotero 端 `.xpi` 文件（文件名通常含 `zotero` 字样，**不要下载 Obsidian 端的**）
3. Zotero → `工具` → `插件` → 齿轮 ⚙️ → `Install Add-on From File...`
4. 选择 `.xpi` → 重启 Zotero

### 2.4 确认本地 API 已启用

1. Zotero → `编辑` → `首选项` → `高级` → `General`
2. 确认勾选了 **`Allow other applications on this computer to communicate with Zotero`**
3. 默认端口为 **23119**（你的导入指南确认已启用，此步仅复核）

---

## 3. Obsidian 端配置

### 3.1 安装 Obsidian

1. 访问 https://obsidian.md/download 下载 Windows 版
2. 安装后打开 Obsidian
3. 选择 **`Open folder as vault`**（打开文件夹作为仓库）
4. 选择路径：**`d:\Desktop\论文\obsidian-vault`**
5. 确认能看到 `Literature`、`Concepts`、`MOC`、`Reviews`、`Templates` 等文件夹

### 3.2 安装 4 个核心插件

> Obsidian → `设置`（齿轮图标）→ `Community plugins` → 如提示关闭安全模式则关闭

#### 插件 1：ZotLit（Zotero 集成）

1. `Community plugins` → `Browse` → 搜索 **`ZotLit`**
2. 点击 `Install` → `Enable`
3. **配置**（设置 → ZotLit）：
   - **Zotero local API URL**：`http://localhost:23119`
   - **Literature note path**：`Literature/{{citekey}}.md`
   - **Literature note template**：`Templates/literature-note.md`
   - **Annotation import**：开启

#### 插件 2：Templater（模板引擎）

1. 搜索 **`Templater`** → Install → Enable
2. **配置**（设置 → Templater）：
   - **Template folder location**：`Templates`
   - **Trigger Templater on new file creation**：开启
   - **Folder Templates**（可选）：
     - `Literature/*` → `literature-note.md`
     - `Concepts/*` → `concept-note.md`

#### 插件 3：Dataview（数据查询）

1. 搜索 **`Dataview`** → Install → Enable
2. **配置**（设置 → Dataview）：
   - **Enable JavaScript Queries**：开启（用于复杂查询）
   - **Enable Inline JavaScript Queries**：开启
   - 其他保持默认

#### 插件 4：Spaced Repetition（间隔重复）

1. 搜索 **`Spaced Repetition`** → Install → Enable
2. **配置**（设置 → Spaced Repetition）：
   - **Algorithm**：`SM-2`（与 Anki 一致，稳定）
   - **Flashcard tags**：添加以下标签（每行一个）：
     ```
     #flashcards
     #flashcards/literature
     #flashcards/recap
     #flashcards/concept
     ```
   - **Store schedule data in**：`Note's frontmatter`（复习数据存入笔记 frontmatter）
   - **Convert folders to decks**：关闭（用标签管理即可）

### 3.3（可选）安装辅助插件

| 插件 | 用途 | 是否必需 |
|------|------|---------|
| Shell Commands | 在 Obsidian 内调用 Python 脚本 | 可选 |
| Excalidraw | 手绘概念图 | 可选 |
| Graph Analysis | 引用网络可视化 | 可选 |

### 3.4 小批量验证

1. 在 Zotero 中选 1 篇高引论文（如 Vaswani 2017）
2. 在 Obsidian 中按 `Ctrl+P` → 搜索 `ZotLit: Create literature note`
3. 选择该论文 → 确认生成了 `Literature/@vaswani2017attention.md`
4. 打开该文件，检查：
   - frontmatter 含 citekey/title/authors/year/venue/doi
   - `## Annotations` 区有 Zotero 中的高亮标注
   - `## 间隔重复卡片` 区有 `#flashcards` 标签
5. 打开 `MOC/00-Dashboard.md`，确认 Dataview 查询能显示该笔记

---

## 4. TRAE 端配置

### 4.1 确认 TRAE 已识别 8 个 Skill

1. 在 TRAE 中打开项目 `d:\Desktop\论文\`
2. 检查 `.trae/skills/` 目录下有 8 个子文件夹：
   - `reading-paper-pre-questions`
   - `recapping-paper-feynman`
   - `linking-paper-concepts`
   - `tracing-lineage-by-era`
   - `tracing-lineage-by-team`
   - `synthesizing-literature-review`
   - `building-citation-timeline`
   - `fact-checking-citations`
3. 每个 Skill 子文件夹内有 `SKILL.md` 文件
4. TRAE 设置 → 技能与命令 → 确认项目页签下显示这些 Skill

### 4.2 申请 Semantic Scholar API Key

**作用**：提升 API 限速（无 key 约 100 请求/5分钟 → 有 key 约 1 请求/秒），对宏观 Skill 的批量检索至关重要。

1. 访问 https://www.semanticscholar.org/product/api#api-key
2. 填写申请表（姓名、邮箱、用途说明如"academic research literature management"）
3. 通常几分钟内收到 API Key 邮件
4. **设置环境变量**：
   - Windows 搜索"环境变量" → `编辑系统环境变量` → `环境变量`
   - 新建用户变量：
     - 变量名：`S2_API_KEY`
     - 变量值：你收到的 API Key
   - 确定保存
   - **重启 TRAE** 使环境变量生效

### 4.3 设置 Crossref Polite Pool 邮箱（可选但推荐）

**作用**：进入 Crossref 的 polite pool，请求更快更稳定。

1. Windows 环境变量 → 新建用户变量：
   - 变量名：`CROSSREF_MAILTO`
   - 变量值：你的真实邮箱（如 `yourname@university.edu`）
2. 确定保存

### 4.4 确认 Python 环境

1. 打开 PowerShell（TRAE 内终端即可）
2. 执行：
   ```powershell
   python --version    # 应 ≥ 3.10
   pip show requests   # 应显示已安装
   ```
3. 如 `requests` 未安装：`pip install requests`

---

## 5. 验证

### 5.1 脚本验证（已完成 ✓）

以下脚本已用真实 DOI 测试通过：

| 脚本 | 测试 DOI | 结果 |
|------|---------|------|
| `crossref_verify.py` | 10.1145/3442188.3445922 | ✅ VERIFIED（含156条参考文献） |
| `s2_verify.py` | 10.1145/3442188.3445922 | ✅ VERIFIED（citationCount 7969） |

### 5.2 Skill 触发验证

在 TRAE 对话中依次测试以下触发短语，确认正确的 Skill 被激活：

| 测试输入 | 预期激活的 Skill |
|---------|-----------------|
| "预读 vaswani2017" | `reading-paper-pre-questions` |
| "费曼复述 vaswani2017" | `recapping-paper-feynman` |
| "关联 vaswani2017" | `linking-paper-concepts` |
| "attention机制的分年代脉络" | `tracing-lineage-by-era` |
| "Hinton的团队脉络" | `tracing-lineage-by-team` |
| "综述这几篇 transformer 论文" | `synthesizing-literature-review` |
| "vaswani2017的引用时间线" | `building-citation-timeline` |
| "引用反查这段文字" | `fact-checking-citations` |

### 5.3 端到端验证

1. **微观流程**：选 1 篇代表作（如 Vaswani 2017）
   - Obsidian 用 ZotLit 生成 `Literature/@citekey.md`
   - TRAE："预读 citekey" → 确认 `## 预读问题` 被填充
   - 精读 PDF
   - TRAE："费曼复述 citekey"（粘贴你的复述）→ 确认 `## 费曼复述` 被填充
   - TRAE："关联 citekey" → 确认 `## 关联` 被填充
   - 在 note 的 `## 间隔重复卡片` 区手写 2 张卡
   - 确认 SR 插件能识别卡片

2. **宏观流程**：选 1 个主题（如 "graph neural network"）
   - TRAE："graph neural network 的分年代脉络"
   - 确认生成 `Reviews/graph-neural-network-lineage.md`
   - 确认所有引用经 `fact-checking-citations` 校验为 VERIFIED

---

## 6. 端到端使用示例

### 精读单篇论文

```
步骤 1：Obsidian → Ctrl+P → "ZotLit: Create literature note" → 选论文
步骤 2：TRAE 对话："预读 vaswani2017attention"
         → Skill 生成三层提问，写入 note 的 ## 预读问题
步骤 3：在 Zotero 内精读 PDF，用四色高亮标注
         → ZotLit 自动同步标注到 note 的 ## Annotations
步骤 4：TRAE 对话："费曼复述 vaswani2017attention"
         粘贴你的大白话复述
         → Skill 逐句批注 + 漏洞提示，写入 ## 费曼复述
步骤 5：TRAE 对话："关联 vaswani2017attention"
         → Skill 四路检索，写入 ## 关联（含 [[wikilink]]）
步骤 6：在 note 的 ## 间隔重复卡片 区手写 2-3 张卡
步骤 7：每日 Obsidian SR 刷卡复习
```

### 做研究脉络综述

```
步骤 1：TRAE 对话："attention 机制的分年代脉络"
         → tracing-lineage-by-era 调 S2 搜索 + 本地索引
         → 按年代分段，每段选代表作
         → 所有引用经 fact-checking-citations 校验
         → 生成 Reviews/attention-lineage.md
步骤 2：TRAE 对话："综述这些 transformer 论文"
         → synthesizing-literature-review 聚类 + 生成综述骨架
         → 每个引用经反查校验
         → 生成 Reviews/transformer-review.md
步骤 3：TRAE 对话："vaswani2017 的引用时间线"
         → building-citation-timeline 生成前后向引用图
         → 生成 Reviews/vaswani2017-timeline.md
步骤 4：在 Obsidian 中打开 Reviews 文件夹，查看所有产物
```

---

## 7. 常见问题

### Q: ZotLit 生成的 literature note 没有 annotation？

**A**: 确认在 Zotero 中已用内置 PDF 阅读器标注了该论文。ZotLit 只同步 Zotero 内置阅读器的标注，不同步外部 PDF 阅读器的标注。

### Q: Dataview 查询显示为空？

**A**: 
1. 确认 Dataview 插件已启用
2. 确认 literature note 的 frontmatter 字段名与 Dataview 查询一致（citekey/year/venue/area/citation_count）
3. 确认 note 在 `Literature/` 文件夹下

### Q: Python 脚本报错 `ModuleNotFoundError: No module named 'requests'`？

**A**: 执行 `pip install requests`。如果你用 conda 环境，确保在正确的环境中安装。

### Q: Semantic Scholar API 返回 429（限速）？

**A**: 
1. 确认已设置 `S2_API_KEY` 环境变量并重启 TRAE
2. 脚本已内置重试逻辑（指数退避）
3. 无 key 时间隔 3 秒，耐心等待即可

### Q: Skill 没有被触发？

**A**:
1. 确认 `.trae/skills/` 下的 SKILL.md frontmatter 格式正确（仅 `name` + `description`）
2. 尝试用更接近 description 中触发词的表述
3. 检查 TRAE 设置 → 技能与命令 → 确认 Skill 已启用

### Q: 1.4 万篇全量导入 Obsidian 会卡吗？

**A**: **不会，也不需要全量导入。** ZotLit 是按需生成——你读哪篇才生成哪篇的 note。`_index.csv` 留在 TRAE 侧作为检索索引，不进 vault。Vault 内只保留你实际读过的论文笔记。

### Q: 间隔重复卡片怎么复习？

**A**: Obsidian 命令面板（`Ctrl+P`）→ 搜索 `SR: Review flashcards in vault` → 进入复习界面。每日刷卡，复习数据自动存入笔记 frontmatter。

---

## 附录：8 个 Skill 速查表

| Skill | 触发词 | 做什么 |
|-------|--------|--------|
| `reading-paper-pre-questions` | 预读/读前提问 | 读前生成三层提问（激活/预测/批判） |
| `recapping-paper-feynman` | 费曼复述 | 校验你的复述，标注漏洞，不给答案 |
| `linking-paper-concepts` | 关联 | 四路检索相关论文（同源/同人/同引/同义） |
| `tracing-lineage-by-era` | 脉络/分年代 | 按年代追溯研究演进 |
| `tracing-lineage-by-team` | 团队脉络 | 追溯某作者/团队的研究线 |
| `synthesizing-literature-review` | 综述 | 合成结构化文献综述 |
| `building-citation-timeline` | 引用时间线 | 生成前后向引用图 |
| `fact-checking-citations` | 引用反查/防幻觉 | 核对引用真实性与论断准确性 |
